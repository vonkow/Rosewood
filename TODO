Add ent.move, and ent.position() functions for moving ent, instea of using ent.base.velX etc
(half-done, see line below)Reorder hitmap ['name', x1, y1, x2, y2, x3, y3]
Add triangular hitmaps and collision detection

Add audio

Add support for determining 'board' div location onscreen and adjusting all placements to it.
attach a recalc function to window.onresize or whatever event it is.

Canvas support someday

Node.js server support someday soon
